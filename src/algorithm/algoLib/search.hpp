#include <vector>

namespace pxu
{
/*
 * Doing binary search for target in sorted array, return the index of the
 * target value. If not exists, return -1
 */
int binarySearch(const std::vector<int> &vec, int target);
}